# URL of the backend API (see /server folder). Leave blank to use the same origin
# (e.g. when the backend is proxied behind the same domain in production, such as
# https://website-staging.decirclesolar.com/api or https://decirclesolar.com/api).
VITE_API_BASE_URL=http://localhost:6100
